# Core ML and RAG frameworks
google-genai
pinecone
pinecone-text
openai

# Document parsing & Chunks
langchain-text-splitters
pymupdf                    # EXTRACT_METHOD=pymupdf: local, no-AI PDF text extraction
tiktoken

# API & Web Server
fastapi
uvicorn
python-multipart
pydantic
requests
python-dotenv

# Database (Neon / Postgres) & Auth
sqlalchemy
psycopg2-binary
bcrypt
pyjwt
slowapi

# Data & Utilities
pandas
numpy
multiprocess

# Observability — openinference instrumentor -> OTLP, exporting to Langfuse
# and/or Grafana (no `langfuse` package). LLM tracing (LANGFUSE_* / GRAFANA_OTLP_*);
# HTTP tracing + metrics (GRAFANA_OTLP_*). All no-ops unless the env vars are set.
openinference-instrumentation-google-genai==1.2.0
opentelemetry-instrumentation-fastapi==0.65b0
opentelemetry-sdk==1.44.0
opentelemetry-exporter-otlp-proto-http==1.44.0